id: curl
name: "curl"
type: binary
command: "curl"
version_check: "curl --version"
description: "HTTP client for ad-hoc requests. Often easier than full MCP integration for one-off endpoints."
required_capabilities:
  - shell:execute
  - network:http_get
tags:
  - http
  - core
